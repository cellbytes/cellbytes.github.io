---
import Section from "~src/components/Section.astro";
import LinkButton from "~src/components/LinkButton.astro";
import H from "~src/components/HeadingLink.astro";
import { contactHref, type ContactTopic } from "~src/utils/contactTopics";

interface Props {
  title: string;
  /** Which draft message the contact form opens with. */
  topic: ContactTopic;
  demo?: boolean;
}
const { title, topic, demo } = Astro.props;
---

<Section class="call-to-action">
  <div class="text">
    <H level="2">{title}</H>
    <p>Contact us to explore how Cellbytes can enhance your daily workflow.</p>
  </div>
  <div class="buttons">
    {
      demo && (
        <LinkButton href="https://demo.cellbytes.app" variant="highlight">
          Try the demo
        </LinkButton>
      )
    }
    <LinkButton
      href={contactHref(topic)}
      variant={demo ? "default" : "highlight"}
    >
      Contact us
    </LinkButton>
  </div>
</Section>

<style>
  h2 {
    text-align: center;
  }
  p {
    font-size: var(--text-size-ingress);
  }
  .call-to-action {
    gap: var(--element-padding);
  }
  .buttons {
    display: flex;
    gap: var(--element-padding);
    flex-wrap: wrap;
    justify-content: center;
  }
</style>
